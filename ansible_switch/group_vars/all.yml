---
# All switches run locally — no real SSH required
ansible_connection: local
ansible_python_interpreter: "{{ ansible_playbook_python }}"

# Default dummy gateway
default_gateway: "10.0.0.1"
subnet_mask: "255.255.255.0"
